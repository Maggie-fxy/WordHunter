# WordCaps 🎯

一款结合现实互动的英语单词"寻宝" Web App。通过拍摄现实中的物体来学习英语单词！

## ✨ 功能特点

- **分屏交互设计**: 上方显示单词和收集网格，下方是相机区域
- **AI 物体识别**: 使用豆包视觉大模型识别拍摄的物体
- **游戏化学习**: 收集 6 个不同的物体来完成一个单词的学习
- **难度分级**: 单词分为 3 个难度等级，获得不同碎片奖励
- **精美动画**: 使用 Framer Motion 实现流畅的交互动画

## 🚀 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 配置环境变量

复制 `.env.local.example` 为 `.env.local` 并填入你的 API 密钥：

```bash
cp .env.local.example .env.local
```

配置项：
- `DOUBAO_API_KEY`: 豆包视觉模型 API Key
- `DOUBAO_MODEL_ID`: 模型 ID (默认: doubao-vision-pro-32k)

> 💡 如果不配置 API Key，将使用模拟模式进行开发测试

### 3. 启动开发服务器

```bash
npm run dev
```

访问 http://localhost:3000 开始游戏！

## 📱 使用方法

1. 查看当前单词（如 "CUP"）
2. 点击"开始寻找"按钮激活相机
3. 在现实中找到对应物体并拍照
4. AI 会判断拍摄的物体是否匹配
5. 收集 6 个不同的物体完成任务
6. 获得碎片奖励，继续下一个单词！

## 🛠 技术栈

- **Frontend**: Next.js 14 (App Router)
- **Styling**: Tailwind CSS
- **Animation**: Framer Motion
- **Icons**: Lucide React
- **AI Vision**: Volcengine Doubao-Vision-Pro
- **Backend**: Firebase (可选)

## 📁 项目结构

```
src/
├── app/
│   ├── api/recognize/    # AI 识别 API
│   ├── layout.tsx        # 根布局
│   └── page.tsx          # 主页面
├── components/
│   ├── ActionArea.tsx    # 底部交互区
│   ├── CameraView.tsx    # 相机视图
│   ├── CollectionGrid.tsx # 收集网格
│   ├── VictoryModal.tsx  # 胜利弹窗
│   └── WordCard.tsx      # 单词卡片
├── context/
│   └── GameContext.tsx   # 游戏状态管理
├── data/
│   └── wordBank.ts       # 词库数据
├── hooks/
│   ├── useCamera.ts      # 相机 Hook
│   └── useTTS.ts         # 语音合成 Hook
├── lib/
│   └── firebase.ts       # Firebase 配置
└── types/
    └── index.ts          # 类型定义
```

## 🎮 游戏机制

### 难度等级
- ⭐ 简单: +1 碎片/次
- ⭐⭐ 中等: +2 碎片/次
- ⭐⭐⭐ 困难: +3 碎片/次

### 兜底机制
- AI 识别失败时可强制通过
- 开发模式自动使用模拟 AI

## 📄 License

MIT
# wordscap
